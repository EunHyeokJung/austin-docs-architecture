# TODO List

> One-line summaries of deferred tasks stored in `docs/todo`.  
> Agents must check this file before starting work; if a related item exists, read the full TODO document first.  
> When a TODO is completed, delete its file, write a work record in `docs/reports/`, and remove it from this list.

<!-- The entry below is an example. Replace it with your actual project items. -->
- [TODO] `signup-complete-access-control.md` | Trigger: when login/signup API is connected | Goal: Block back-navigation and re-entry after signup completion; redirect invalid access to login
